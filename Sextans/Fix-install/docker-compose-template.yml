services:

  graphdb:
    image: fairdatasystems/gdb:2026-02-11
    restart: always
    hostname: graphdb
    ports:
      - 127.0.0.1:{GDB_PORT}:7200
    volumes:
      - {PREFIX}-graphdb:/opt/graphdb/home
    networks:
      - {PREFIX}-default

      
  transformation-daemon:
    image:  fairdatasystems/cdeb:2026-02-11    # to use the CARE-SM models with teh care-sm-toolkit
    environment:
      GraphDB_User: ${GraphDB_User}
      GraphDB_Pass: ${GraphDB_Pass}
      baseURI: ${baseURI}
      GRAPHDB_REPONAME: ${GRAPHDB_REPONAME}
    depends_on:
      - caresm
      - yarrrml-rdfizer
    ports:
      - 127.0.0.1:{RDF_TRIGGER}:4567
    volumes:
      - ./data:/data
    networks:
      - {PREFIX}-default

  caresm:
    image: saracuriel/care:1.0.0 #Check latest tag at https://hub.docker.com/repository/docker/saracuriel/care/tags
    hostname: caresm
    volumes:
      - ./data:/data
    networks:
      - {PREFIX}-default

  yarrrml-rdfizer:
    image: fairdatasystems/yrml:2026-02-11
    hostname: yarrrml-rdfizer
    environment:
      # (nquads (default), trig, trix, jsonld, hdt, turtle)
      - SERIALIZATION=nquads
#    ports:
#      - "4567:4567"
    volumes:
      - ./data:/mnt/data
    networks:
      - {PREFIX}-default


volumes:
  {PREFIX}-graphdb:
    external: true

networks:
  {PREFIX}-default: